

const SectionTitle = ({title}) => {
    return (
        <div>
             <h2 className='font-catamaran text-base md:text-2xl font-bold text-center uppercase py-8'>{title}</h2>
        </div>
    );
};

export default SectionTitle;